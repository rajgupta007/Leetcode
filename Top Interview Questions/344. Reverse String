class Solution {
public:
    void reverseString(vector<char>& s) {
        
        int sizeof_s=s.size();
        for(int i=0; i<sizeof_s/2; i++)
        {
            char temp = s[i];
            s[i]=s[sizeof_s-1-i];
            s[sizeof_s-1-i]=temp;
        }        
    }
};
