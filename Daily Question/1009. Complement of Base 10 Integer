class Solution {
public:
    int bitwiseComplement(int n) {
        if(n==0)return 1;
        int numberofbits=(int)floor(log10(n)/log10(2))+1;
        int p = pow(2,numberofbits)-1;
        return (n^p);
        
        }
};
